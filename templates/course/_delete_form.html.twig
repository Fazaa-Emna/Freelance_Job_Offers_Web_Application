<form method="post" action="{{ path('app_course_delete', {'cid': course.cid}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ course.cid) }}">
    <button class="btn">Delete</button>
</form>
