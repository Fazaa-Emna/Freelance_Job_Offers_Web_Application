<form method="post" action="{{ path('app_freelance_delete', {'idfreelance': freelance.idfreelance}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ freelance.idfreelance) }}">
    <button class="btn float-right">Delete</button>
</form>
