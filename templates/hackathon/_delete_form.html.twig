<form method="post" action="{{ path('app_hackathon_delete', {'event': hackathon.event.eventId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ hackathon.event) }}">
    <button class="btn">Delete</button>
</form>
