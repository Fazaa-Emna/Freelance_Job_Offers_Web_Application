{% extends 'base.html.twig' %}

{% block title %}Application{% endblock %}

{% block body %}
    <h1>Application</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Idapp</th>
                <td>{{ application.idapp }}</td>
            </tr>
            <tr>
                <th>Idfreelancer</th>
                <td>{{ application.idfreelancer }}</td>
            </tr>
            <tr>
                <th>Idbo</th>
                <td>{{ application.idbo }}</td>
            </tr>
            <tr>
                <th>FnFreelancer</th>
                <td>{{ application.fnFreelancer }}</td>
            </tr>
            <tr>
                <th>LnFreelancer</th>
                <td>{{ application.lnFreelancer }}</td>
            </tr>
            <tr>
                <th>Emailfreelancer</th>
                <td>{{ application.emailfreelancer }}</td>
            </tr>
            <tr>
                <th>Applicationdate</th>
                <td>{{ application.applicationdate ? application.applicationdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UrlCv</th>
                <td>{{ application.urlCv }}</td>
            </tr>
            <tr>
                <th>Confirmation</th>
                <td>{{ application.confirmation ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Notification</th>
                <td>{{ application.notification ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_application_index') }}">back to list</a>

    <a href="{{ path('app_application_edit', {'idapp': application.idapp}) }}">edit</a>

    {{ include('application/_delete_form.html.twig') }}
{% endblock %}
