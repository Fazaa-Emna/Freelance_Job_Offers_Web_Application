<form method="post" action="{{ path('app_workshop_delete', {'event': workshop.event.eventId}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ workshop.event) }}">
    <button class="btn">Delete</button>
</form>
