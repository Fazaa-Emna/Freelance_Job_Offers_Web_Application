{% extends 'base.html.twig' %}

{% block title %}Package {% endblock %}

{% block body %}

 


     <main>

        <!-- Hero Area Start-->
        <div class="slider-area ">
        <div class="single-slider section-overly slider-height2 d-flex align-items-center"  style="background-image:url('{{ asset('./assets/img/hero/ny.jpg') }}')">
            <div class="container">
                <div class="row">
                    <div class="col-xl-12">
                        <div class="hero-cap text-center">
                            <h2>Your Packages </h2>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        </div>
        <!-- Hero Area End -->
        <!-- job post company Start -->
        <div class="job-post-company pt-120 pb-120">
            <div class="container">
                <div class="row justify-content-between">
                    <!-- Left Content -->
                    <div class="col-xl-7 col-lg-8">
                        <!-- job single -->
                       
                          <!-- job single End -->
                       
                        <div class="job-post-details">
                            <div class="post-details1 mb-50">
    <h1>Packages</h1>

    <table class="table">
        <thead>
            <tr>
                <th>IdP</th>
                <th>Type</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for packag in packags %}
            <tr>
                <td>{{ packag.idP }}</td>
                <td>{{ packag.type }}</td>
                <td>{{ packag.price }}</td>
                <td>
                    <a href="{{ path('app_packag_show', {'idP': packag.idP}) }}" class="genric-btn primary radius">show</a>
                    <a href="{{ path('app_packag_edit', {'idP': packag.idP}) }}"class="genric-btn primary radius">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_packag_new') }}" class="genric-btn primary radius">Create new</a>
    <div class="post-details2  mb-50">
                                 <!-- Small Section Tittle -->
 
                              
                            </div>
                           
                        </div>

                    </div>
                    <!-- Right Content -->
                    
                       
                    </div>
                </div>
            </div>
        </div>
        <!-- job post company End -->

    </main>



{% endblock %}


