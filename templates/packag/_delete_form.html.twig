<form method="post" action="{{ path('app_packag_delete', {'idP': packag.idP}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ packag.idP) }}">
    <button class="btn" >Delete</button>
</form>
